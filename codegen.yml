overwrite: true
# schema: "http://localhost:4000"
schema:
  - src/schema/**/*.typeDefs.gql
documents: null
generates:
  src/shared/index.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    hooks:
      afterOnFileWrite:
        - 'prettier --write'
