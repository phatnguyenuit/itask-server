overwrite: true
# schema: "http://localhost:4000"
schema:
  - src/schema/**/*.typeDefs.gql
documents: null
generates:
  src/generated/graphql.types.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    hooks:
      afterOnFileWrite:
        - 'prettier --write'
